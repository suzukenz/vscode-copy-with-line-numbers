`Copy with Line Numbers` のコマンドを実行すると、選択したコードを行番号付きでクリップボードにコピーするシンプルなVS Code拡張機能です。

`{行番号}: {コード}` の形式でコードをクリップボードにコピーします。

例:

```
51: /// <summary>
52: /// Initializes a new instance of the <see cref="VSPackage"/> class.
53: /// </summary>
```
